name: Print

on:
  push:
    branches:
      - main

jobs:
  print:
    runs-on: ubuntu-latest

    steps:
      - name: Print code
        run: |
          ls -la
          chmod +x ${GITHUB_WORKSPACE}/run.sh
          ${GITHUB_WORKSPACE}/run.sh
        shell: bash